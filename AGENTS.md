# Portfolio

Greenfield portfolio website.

## Skills (locked via skills-lock.json)

- **frontend-design** — distinctive, production-grade frontend interfaces. Requires bold aesthetic direction before coding. No generic AI aesthetics (Inter, Roboto, purple gradients). Load via `skill frontend-design`.
- **css-animations** — HyperFrames CSS animation adapter patterns. Finite `animation-iteration-count`, `animation-fill-mode: both`, `data-start` attributes. Validate with `npx hyperframes lint` and `npx hyperframes validate`.

## Setup

No `package.json` or build config yet. Initialize tooling when first source file is created.